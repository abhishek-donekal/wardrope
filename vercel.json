{
  "buildCommand": "cd frontend && yarn install && npx expo export --platform web",
  "outputDirectory": "frontend/dist",
  "framework": null,
  "cleanUrls": true,
  "rewrites": [{ "source": "/(.*)", "destination": "/index.html" }]
}
